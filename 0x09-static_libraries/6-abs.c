#include "main.h"

/**
 * _abs - show the absolute value of an integer.
 *
 * @n: an integer parameter.
 *
 * Return: n if n is positive, -n otherwise.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}

