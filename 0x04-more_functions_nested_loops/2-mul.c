#include "main.h"
/**
 * mul - function to multiply 2 integers.
 * @a: first integers param.
 * @b: second integers param.
 * Return: product of a and b.
 */

int mul(int a, int b)
{
	return (a * b);
}

